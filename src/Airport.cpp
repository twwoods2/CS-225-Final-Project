#include "Airport.h"
// ostream method
// outputs the node data into terminal
/*
std::ostream& Airport::operator<<(std::ostream& os, const Airport& node) {
    os << "Airport Name: " << node.airport_name_ << ", Airport ID: " << source.id_
     << ", latitude: " << source.lat_ << ", longitude: " << source.longit_; 
     return os;
}
*/